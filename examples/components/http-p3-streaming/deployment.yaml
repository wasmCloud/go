# Deploys this async WASI P3 component on wasmCloud v2. The manifest has the
# same shape as a P2 one: the host serves wasi:http/handler@0.3.0 behind the
# same `incoming-handler` entry.
#
#   kubectl apply -f deployment.yaml
#
# See https://wasmcloud.com/docs/quickstart/deploy-a-webassembly-workload/
apiVersion: v1
kind: Service
metadata:
  name: http-p3-streaming
spec:
  type: NodePort
  ports:
    # The port must be named `http`: the operator writes this selectorless
    # Service's EndpointSlices with a port named `http`, and Kubernetes
    # matches slice ports to Service ports by name. Unnamed, nothing routes.
    - name: http
      port: 80
      protocol: TCP
---
apiVersion: runtime.wasmcloud.dev/v1alpha1
kind: WorkloadDeployment
metadata:
  name: http-p3-streaming
spec:
  replicas: 1
  template:
    spec:
      hostSelector:
        hostgroup: default
      kubernetes:
        service:
          name: http-p3-streaming
      components:
        - name: http-p3-streaming
          image: ghcr.io/[your-org]/http-p3-streaming:0.1.0
          localResources:
            # Outbound HTTP is deny-by-default on a real host: with no
            # allowedHosts every request fails. This mirrors
            # workload.allowedHosts in .wash/config.yaml, which only `wash dev`
            # reads.
            allowedHosts:
              - dog.ceo
      hostInterfaces:
        - namespace: wasi
          package: http
          interfaces:
            - incoming-handler
          config:
            host: localhost
