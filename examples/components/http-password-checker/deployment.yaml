# Deploys this component on wasmCloud v2 (runtime operator >= 2.5.2).
#
#   kubectl apply -f deployment.yaml
#
# See https://wasmcloud.com/docs/quickstart/deploy-a-webassembly-workload/
apiVersion: v1
kind: Service
metadata:
  name: http-password-checker
spec:
  type: NodePort
  ports:
    # The port must be named `http`: the operator writes this selectorless
    # Service's EndpointSlices with a port named `http`, and Kubernetes
    # matches slice ports to Service ports by name. Unnamed, nothing routes.
    - name: http
      port: 80
      protocol: TCP
---
apiVersion: runtime.wasmcloud.dev/v1alpha1
kind: WorkloadDeployment
metadata:
  name: http-password-checker
spec:
  replicas: 1
  template:
    spec:
      hostSelector:
        hostgroup: default
      kubernetes:
        service:
          name: http-password-checker
      components:
        - name: http-password-checker
          image: ghcr.io/[your-org]/http-password-checker:0.1.0
      hostInterfaces:
        - namespace: wasi
          package: http
          interfaces:
            - incoming-handler
          config:
            host: localhost
