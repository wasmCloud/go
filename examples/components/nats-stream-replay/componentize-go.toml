# Adds this component's extra world (wasmcloud:nats/jetstream) on top of the
# SDK's default wasip2 world; componentize-go merges worlds from all
# componentize-go.toml files found in the module graph.
worlds = ["wasmcloud:examples/nats-stream-replay@0.1.0"]
wit_paths = ["wit"]
